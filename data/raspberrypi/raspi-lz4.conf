# zstd compression is extremely slow on the Pi, but also takes up far
# too much memory, resulting in OOM on the 512MB platforms (Pi Zero 2,
# and the 3A+). Use the old lz4 default on the Pi (LP: #1950214)

COMPRESS=lz4
